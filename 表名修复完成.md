# ✅ 数据库表名修复完成

**错误：** `relation "public.point_history" does not exist`  
**原因：** 之前重命名了表 `point_history` → `ranking_point_history`，但前端代码没有全部更新  
**状态：** ✅ 已全部修复

---

## 🔄 修改的文件

### 1. `src/views/AdminDashboard.vue`
```javascript
// 之前
.from('point_history')

// 现在
.from('ranking_point_history')
```

### 2. `src/views/PlayersPage.vue`
```javascript
// 2处修改
.from('point_history') → .from('ranking_point_history')
```

### 3. `src/views/ProfilePage.vue`
```javascript
// 1处修改
.from('point_history') → .from('ranking_point_history')
```

### 4. `src/views/LeaderboardPage.vue`
✅ 已经是正确的（之前修复过）

---

## ✅ 修复完成

所有对 `point_history` 的引用都已更新为 `ranking_point_history`

**现在刷新浏览器，错误应该消失了！** 🎉

---

## 📝 表名变更总结

| 旧表名 | 新表名 | 用途 |
|--------|--------|------|
| `point_history` | `ranking_point_history` | 段位积分历史（影响排名） |
| - | `loyalty_point_history` | 消费积分历史（会员福利）|

这样命名更清晰，不会混淆！

